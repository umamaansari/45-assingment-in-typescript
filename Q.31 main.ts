let userName:string[]=[`admin`,`ayesha`, `muniza`,`ismat`,`fiza`];

if(userName.length===0){
    console.log(`we need to find some users!`);
} else{
    userName =[];
    console.log(`all user Names have been reserved .`)
}